[build]
root = "video_downloader"

[deploy]
startCommand = "python manage.py migrate --noinput && gunicorn video_downloader.wsgi:application --bind 0.0.0.0:$PORT --workers 3 --timeout 120 --log-level debug --access-logfile - --error-logfile -"